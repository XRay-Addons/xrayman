// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: settings.sql

package queries

import (
	"context"
	"encoding/json"
)

const ensureSettings = `-- name: EnsureSettings :exec
INSERT INTO settings (id, settings, updated_at)
VALUES (true, $1::jsonb, now())
ON CONFLICT (id)
DO NOTHING
`

func (q *Queries) EnsureSettings(ctx context.Context, cfg json.RawMessage) error {
	_, err := q.db.ExecContext(ctx, ensureSettings, cfg)
	return err
}

const getSettings = `-- name: GetSettings :one
SELECT settings
FROM settings
WHERE id = true
`

func (q *Queries) GetSettings(ctx context.Context) (json.RawMessage, error) {
	row := q.db.QueryRowContext(ctx, getSettings)
	var settings json.RawMessage
	err := row.Scan(&settings)
	return settings, err
}

const setSettings = `-- name: SetSettings :exec
INSERT INTO settings (id, settings, updated_at)
VALUES (true, $1::jsonb, now())
ON CONFLICT (id)
DO UPDATE SET
    settings = EXCLUDED.settings,
    updated_at = now()
`

func (q *Queries) SetSettings(ctx context.Context, cfg json.RawMessage) error {
	_, err := q.db.ExecContext(ctx, setSettings, cfg)
	return err
}
