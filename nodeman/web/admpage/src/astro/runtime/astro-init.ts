/*import { i18nateDOM, startI18nateObserver } from "../../runtime/dom/i18n-dom";
import { LanguageChanged, PaletteChanged } from "@/runtime/events";
import { initPalette, changePalette } from "./palette";

// i18n processing
i18nateDOM();
startI18nateObserver();

document.addEventListener(LanguageChanged, () => {
  i18nateDOM();
});

// palette processing
initPalette();

document.addEventListener(PaletteChanged, () => {
  changePalette();
});*/
