---
const { class: extraClass = "", id = "", part = "", ...rest } = Astro.props;
---

<input
  type="text"
  id={id}
  part={part}
  class={`ui-reset-input w-100 h-100 text-md p-2 ${extraClass}`}
  {...rest}
/>
