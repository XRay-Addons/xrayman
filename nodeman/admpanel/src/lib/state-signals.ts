import { atom } from "nanostores";
/*import { User, ApiReason } from "./types";

export type StateSignal =
  | { status: "idle" }
  | { status: "logged-in"; data: User }
  | { status: "logged-out" }
  | { status: "server-error"; reason: ApiReason };

export const stateSignal = atom<StateSignal>({ status: "idle" });*/
